//#include "trie.h"
//
//trie::trie(int max_siblings, char start_with)
//    : max_siblings(max_siblings), start_with(start_with), root(0) {
//    container.emplace_back(max_siblings); // trie[0]
//}
